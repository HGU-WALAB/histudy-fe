import { Box, styled } from "@mui/material";

export const StyledColumnAlignLayout = styled(Box)({
  minHeight: "100vh",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  marginBottom: "100px",
  marginTop: "50px",
  padding: "0px 50px",
});
