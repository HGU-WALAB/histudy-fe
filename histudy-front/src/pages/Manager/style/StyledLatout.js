import { Box, styled } from "@mui/material";

export const StyledLayout = styled(Box)({
  display: "flex",
  justifyContent: "center",
  gap: "50px",
  padding: "50px 50px",
  minHeight: "100vh",
});
